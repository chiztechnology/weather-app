import React from 'react';
import './menubar.css';

const MenuBar = () => (
  <div className="main-app">
    <p className="logo">Weather App</p>
    {/* config panel */}
  </div>
);

export default MenuBar;
