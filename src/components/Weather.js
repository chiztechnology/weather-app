import React from 'react';
import { Button } from '@mantine/core';

const Weather = () => {
  const a = 0;

  return (
    <>
      <Button>Click me! </Button>
      {a}
    </>
  );
};

export default Weather;
